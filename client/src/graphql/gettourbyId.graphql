query tourByID($tourId:Float!){
    tourByID(tourId:$tourId){
        id
        name
        image
        location
        price
        reviews{
            rating
            description
            user{
                name
            }
        }
    }
}